# Backend API base URL — used when VITE_MOCK_API is not "true".
VITE_API_BASE_URL=http://localhost:8000

# Set to "true" to use the in-browser mock API (no backend required).
# The .env.production file already enables this for the Vercel demo build.
VITE_MOCK_API=false
