# Enable the in-browser mock API for the Vercel demo build.
# When true, src/api/client.ts routes every call through
# src/api/mocks/router.ts and auto-logs-in as the demo admin user.
VITE_MOCK_API=true
