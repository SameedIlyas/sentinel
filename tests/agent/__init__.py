"""Synthetic clinic-admin agent tests (Phase 4).

Hand-rolled deterministic state machine — NOT ``claude-agent-sdk``. This
guarantees the agent harness runs in every CI invocation rather than
silently skipping.
"""
