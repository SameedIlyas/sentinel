"""Clinic-tier route integration tests.

Each ``test_routes_*.py`` covers one route module under
``policy_engine/routes/clinic/``. Auth contract: clinic routes are
JWT-only (verified in tests/clinic/test_auth_contract.py).
"""
