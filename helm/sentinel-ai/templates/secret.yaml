{{/*
Sentinel AI — Secret template

IMPORTANT: This chart does NOT create the Secret.
The Secret must be created externally before deploying, containing:
  SECRET_KEY, DATABASE_URL, REDIS_URL, POSTGRES_PASSWORD, REDIS_PASSWORD

See values.yaml header for the kubectl create secret command.

This template renders a placeholder comment only, to document the requirement.
*/}}
# The Secret "{{ .Values.secretName }}" must be pre-created in namespace {{ .Release.Namespace }}.
# This chart will NOT create or manage it. See Chart.yaml and values.yaml for instructions.
